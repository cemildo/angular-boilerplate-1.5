class ContactController {
  constructor($state) {
    this.name = 'Contact Us';
    this.state = $state;
    // console.log('get all the states',$state.get());
  }
}

export default ContactController;
